// Repository/Releases.tsx
import { EmptyState } from "../../shared/components";

export function Releases() {
  return (
    <EmptyState
      title="Releases aren't wired up yet"
      description="No releases table or GitHub releases sync exists on the backend yet."
    />
  );
}
