// Repository/Tags.tsx
import { EmptyState } from "../../shared/components";

export function Tags() {
  return (
    <EmptyState
      title="Tags aren't wired up yet"
      description="No tags endpoint exists on the backend yet."
    />
  );
}
