// Issues/Milestones.tsx
import { EmptyState } from "../../shared/components";

export function Milestones() {
  return (
    <EmptyState
      title="Milestones aren't tracked yet"
      description="No milestones table or sync exists on the backend yet."
    />
  );
}
